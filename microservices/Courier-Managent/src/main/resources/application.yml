server:
  port: 8081

spring:
  application:
    name: Courier-Managent
  datasource:
    url: jdbc:postgresql://localhost:5432/courier_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
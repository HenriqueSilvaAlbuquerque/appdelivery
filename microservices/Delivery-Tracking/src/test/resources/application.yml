spring:
  application:
    name: delivery-Tracking
  datasource:
    url: jdbc:postgresql://localhost:5432/deliverydb_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true